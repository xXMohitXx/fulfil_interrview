services:
  - type: web
    name: product-management-app
    env: python
    runtime: python-3.11.10
    buildCommand: pip install --upgrade pip && pip install -r requirements-prod.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --log-level info
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:yQssSttRghFbuthG@db.bhyrldeuwxmtaebjpcmu.supabase.co:5432/postgres?sslmode=require
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
      - key: PYTHONUNBUFFERED
        value: "1"